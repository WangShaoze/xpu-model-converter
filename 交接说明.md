# 交接说明：昆仑 XPU 真机接口对齐

> 生成时间：2026-09-10
> 交接原因：用户将把当前代码整体拷贝到局域网麒麟机器上继续开发。
> 本文件是**自包含**的，不依赖任何外部记忆即可读懂。

---

## 0. 一句话结论

**部署链路已跑通，但产物仍是"降级占位件"。** 真正要拿到 XPU 产物，必须把
`KunlunSdkAdapter` 和运行时从**虚构的 Python 模块名**改成**真实的 Paddle Inference XPU 接口**。
真实接口已在本机摸清，证据见 §3、§4。

---

## 1. 麒麟服务器环境（已验证）

| 项 | 值 |
| --- | --- |
| 地址 / 账号 | `192.168.3.87`，`root` / `Pass@#9090` |
| 系统 | Kylin V10 SP1，内核 5.15.0-52-generic，主机名 `compose-pc` |
| Docker | 26.1.3 |
| 部署目录 | `/root/xpu-deploy-test` |
| SSH | **已配置公钥免密**，可直接 `ssh -o BatchMode=yes root@192.168.3.87` |
| XPU 设备 | **无 `/dev/xpuctrl`（本机没有昆仑卡）** |

**关键前提（用户确认）**：本机使用的镜像与真正**有卡的生产服务器是同一个镜像**，
所以内部接口完全一致 —— 这是"在没有卡的机器上做接口对齐"的可行性依据。

**基础镜像**：
```
iregistry.baidu-int.com/ai-station/kunlun2-paddlepaddle_2_6_1-py310-cv:v20260121
```
- 本地已存在（7.04GB），直接可用。
- `iregistry.baidu-int.com` 在本机 **DNS 不通**，只能走本地缓存镜像，不要尝试 pull。

**执行方式约定（用户明确要求）**：用**原生 `ssh` / `scp` 直接操作，全过程可见**。
不要写 paramiko / 自动化脚本代跑。

**Windows → ssh 的引号陷阱（踩过）**：PowerShell 对 `\"...\"` 嵌套转义和嵌套 `|` 会破坏命令。
可靠做法是把 Python 代码 base64 后透传：
```powershell
$code = @'
print("hello")
'@
$b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($code))
ssh -o BatchMode=yes root@192.168.3.87 "echo $b64 | base64 -d | docker exec -i xpu-probe python3 -"
```

---

## 2. 致命发现：昆仑 XPU SDK 是 C/C++ 库，不是 Python 模块

这是**整个转换链路之前走不通的根本原因**。

```
/usr/local/xpu  ->  /usr/local/xpu-4.31.0        （符号链接，SDK 版本 4.31.0）
/usr/local/xpu-4.31.0/lib64/libxpurt.so          XPU Runtime
/usr/local/xpu-4.31.0/lib64/libxpuml.so          XPU ML
/usr/local/xpu-4.31.0/include/xpu/               C 头文件
/usr/local/xpu-4.31.0/bin/xpu_smi                XPU 设备管理 CLI（对应 nvidia-smi）
```

**当前代码里的 `XPU_TOOLKIT_MODULES = ("xpu_toolkit", "xtcl", "xpu_inference")` 是虚构的**，
容器里这三个模块**一个都不存在**，因此永远探测失败、永远降级到 `StubSdkAdapter`。

Python 侧接入 XPU 的唯一途径是 **`paddlepaddle-xpu` 提供的 Paddle Inference**，
它内部动态链接 `libxpurt.so` / `libbkcl.so`（已实测：`import paddle` 时会加载这两个库）。

---

## 3. 真实推理接口（实测存在，非推测）

### 3.1 已实测的 API 可用性

容器内 `python3` 实测结果：

```
paddle: 2.6.1
has paddle.device.xpu: True
xpu device_count: 0                     # 本机无卡，故为 0
paddle.inference.Config: True
Config.enable_xpu: True
Config.set_xpu_device_id: True
Config.disable_mkldnn: True
create_predictor: True
```

### 3.2 标准调用形态

```python
import paddle
import paddle.inference as paddle_infer

# 设备探测（无卡返回 0）
device_count = paddle.device.xpu.device_count()

# 构造：必须同时传 计算图 + 权重 两个文件
config = paddle_infer.Config(prog_path, param_path)   # prog=.pdmodel  param=.pdiparams
config.set_optim_cache_dir("/tmp/paddle_cache")

if device == "xpu":
    config.enable_xpu()                  # 注意：不是 enable_xpu(0)
    if xpu_id > 0:
        config.set_xpu_device_id(xpu_id)
else:
    config.disable_mkldnn()              # CPU 模式

predictor = paddle_infer.create_predictor(config)
predictor.get_input_handle(name).copy_from_cpu(blob)
predictor.run()
out = predictor.get_output_handle(name).copy_to_cpu()
```

### 3.3 参考实现的设备策略（厂商真实生产代码）

`C:\WINDOWS\TEMP\yolo26s_ref\yolo26s_dockerimg_v1.0\yolo26s\yolo26s_detector.py`
是目前唯一可用的**事实依据**（厂商交付包内的真实代码）：

```
DEVICE=auto  →  paddle.device.xpu.device_count() > 0 时走 enable_xpu()
                                   否则走 disable_mkldnn()（CPU）
```

**即：设备选择发生在"运行时"，同一份产物在有卡机器走 XPU、无卡机器走 CPU。**

---

## 4. 产物格式真相

参考包 `yolo26s_dockerimg_v1.0` 的实际结构（本地解包副本
`C:\WINDOWS\TEMP\yolo26s_ref\yolo26s_dockerimg_v1.0\`）：

```
yolo26s_dockerimg_v1.0/
├── Dockerfile
├── build.sh
├── install.conf
├── readme.txt
├── testimage.jpg          接口自测图
├── yolo26s.tgz            运行时代码包（ADD 到 /usr/local）
├── packages/              12 个 wheel（无 onnxruntime）
└── yolo26s/
    ├── model/
    │   ├── model.pdmodel      ← Paddle 静态图（计算图）
    │   └── model.pdiparams    ← Paddle 权重
    ├── yolo26s_detector.py    ← 推理实现（事实依据）
    ├── yolo26s_webserver.py
    ├── yolo26s_settings.py
    ├── start.sh
    └── ...
```

**产物是 `model.pdmodel` + `model.pdiparams`，不是 `model.xpu`。**

参考包 Dockerfile 原文（`C:\WINDOWS\TEMP\yolo26s_ref\yolo26s_dockerimg_v1.0\Dockerfile`）：

```dockerfile
# 算法镜像 Dockerfile(由模型转换平台 docker_export 渲染生成)
# 基础镜像与客户服务器一致: 昆仑芯 XPU paddle 2.6.1 运行时
FROM iregistry.baidu-int.com/ai-station/kunlun2-paddlepaddle_2_6_1-py310-cv:v20260121
...
# ---- 运行配置(客户现场可用 docker run -e 覆盖;DEVICE=auto 时无XPU卡自动降级cpu) ----
ENV DEVICE=auto
ENV WEB_PORT=58026
...
COPY packages/* /tmp/packages/
RUN if ls /tmp/packages/*.whl ...; then pip install /tmp/packages/*.whl --no-deps; ...
CMD ["/bin/bash","start.sh"]
```

两个要点：
1. **基础镜像与本机完全同源** —— 推理依赖全部由镜像自带的 paddle 提供，
   交付包只额外装 Flask/gunicorn 等服务化依赖（**不含 onnxruntime**）。
2. **厂商自己的注释就写着「DEVICE=auto 时无XPU卡自动降级cpu」** ——
   即"一份产物在有卡机器走 XPU、无卡机器走 CPU"是**厂商的既定设计**，
   与 §5 的结论完全一致。这正是本项目应该对齐的目标形态。

---

## 5. 架构结论：有卡之后会自动切 XPU 吗？

**用当前这个 stub 包：不会。** 因为降级决策在**转换时**就被写死进
`model/metadata.json` 的 `artifact_format="stub"` / `sdk_adapter="stub"`，
运行时（`runtime/common/runtime_backend.py::RuntimeEngine.load()`）
第 78–81 行的 stub 分支会把加载路径**无条件**锁死在 onnxruntime，与有没有卡无关：

```python
# runtime/common/runtime_backend.py 第 68~84 行
if self.device == "cpu" or xpu_device_count() == 0:   # 第 68 行：无卡/强制 CPU
    if adapter and adapter != "stub" and artifact_format == "xpu":
        raise RuntimeError(...)                        # 第 69~73 行：真 XPU 产物拒绝 CPU 启动
    self._load_onnx_runtime()                          # 第 74 行
    self.degraded = xpu_device_count() == 0
    return self

if adapter == "stub" or artifact_format == "stub":     # 第 78 行：产物是占位件
    self._load_onnx_runtime()                          # 第 79 行  ← 有卡也走这里
    self.degraded = True
    return self

self._load_xpu()                                       # 第 83 行：只有真产物才走到
```

注意第 68 行的判断**先于**第 78 行；而第 78 行在有卡机器上同样成立
（metadata 写的是 stub），所以**插卡也还是 onnxruntime**。

**换成 Paddle 静态图产物后：会。** 因为 Paddle 静态图是**设备无关**的 —— 同一份
`.pdmodel` + `.pdiparams`，有卡时 `enable_xpu()`、无卡时 `disable_mkldnn()`。
这正是参考实现的策略，也是本项目的目标形态。

行为对照：

| 交付包产物 | 目标机有卡 + SDK | 目标机无卡 |
| --- | --- | --- |
| **stub（现状）** | onnxruntime（degraded） | onnxruntime（degraded） |
| **Paddle 静态图（目标）** | XPU | CPU（`disable_mkldnn`）——自动切换 |

---

## 6. 待改代码清单（下一步要做的全部工作）

### 6.1 `runtime/common/runtime_backend.py`（部署包运行时）
- [ ] `XPU_TOOLKIT_MODULES = ("xpu_toolkit", "xtcl", "xpu_inference")` → **删除**，
      改为探测 `paddle` + `paddle.inference`。
- [ ] `xpu_device_count()` → 实现为 `paddle.device.xpu.device_count()`（异常时返回 0）。
- [ ] `_load_xpu()` → 按 §3.2 构造 Paddle Inference predictor
      （`Config(prog, params)` + `enable_xpu()` / `disable_mkldnn()`）。
- [ ] 模型文件解析：现在假定**单文件**产物，需改为**两个文件**
      （`.pdmodel` + `.pdiparams`）。

### 6.2 `xpu_converter/backend/kunlun/runtime.py`
- [ ] `PaddleXpuRuntimeSession` 用的是 `Config(self._model_path)`（**单参**），
      Paddle 需要 prog + params **两参**，需改。
- [ ] 用的是 `config.enable_xpu(0)`，参考实现是 `enable_xpu()` + `set_xpu_device_id(0)`，需对齐。

### 6.3 `xpu_converter/backend/kunlun/compiler.py`
- [ ] `PaddleXpuSdkAdapter.accepts_onnx` 目前是 `False` 且 `compile()` 直接抛错 →
      改为 `True`，并用 **X2Paddle** 实现 `ONNX → Paddle 静态图`。
- [ ] `artifact_format` 从 `"stub"` 改为 `"paddle"`，`degraded` 置 `False`。

### 6.4 `xpu_converter/exporter/manifest.py`
- [ ] `ARTIFACT_FILENAME = "model.xpu"` 与 Paddle 产物不匹配，
      需支持 `model.pdmodel` + `model.pdiparams` 双文件。

### 6.5 转换执行位置（架构决策，未定）
paddle 只存在于**镜像内部**，Windows 项目 `.venv` 里没有。两条路：
- **A（推荐先试）**：转换在麒麟机器的**容器内**执行（镜像里已具备 paddle），
  Windows 侧只负责到 ONNX 为止（步骤 1~6），第 7 步"编译"进容器跑。
- **B**：在 Windows 项目 `.venv` 里装 `paddlepaddle`(CPU) + `x2paddle` + `onnx` + `sympy`，
  让整条流水线在本机一次跑完。**未验证**。

---

## 7. ONNX → Paddle 转换工具链（半验证）

- `x2paddle` 版本 **1.6.0** 可从 `https://pypi.org/simple` 取到。
- **注意：容器默认 pip 源是清华镜像，上面没有 x2paddle**，必须显式
  `-i https://pypi.org/simple`（pypi.org 从服务器可达）。
- `x2paddle 1.6.0` 的依赖**只有 sympy + packaging**。
- 容器内**缺** `onnx`、`sympy`（转换前必须装）；`packaging` 已有（24.0）。
- **未确认**：容器内 `pip install x2paddle onnx sympy` 的实际结果 ——
  该命令在用户叫停时仍在后台运行，结论待补。转换后数值一致性**尚未验证**。

---

## 8. 镜像内 Python 环境现状（实测）

**基础镜像自带**：
```
paddlepaddle-xpu 2.6.1 / paddle2onnx / numpy 1.26.4 /
opencv-python 3.4.18.65 / packaging 24.0 / protobuf 6.33.2
```

**基础镜像不含**（需随交付包 `packages/` 提供）：
```
Flask / gunicorn / onnxruntime / minio / kafka-python
```

---

## 9. 当前部署状态与遗留物

**部署结果：yolov5 交付包已在麒麟上部署成功并可用**（走 onnxruntime 降级）：
- 镜像 `yolov5:v1.0`，容器 `yolov5_v1.0` **Up**，端口 `0.0.0.0:58025->58025/tcp`
- `GET /health` → HTTP 200，`code=0`
- `POST /predict` → HTTP 200，`code=0`，结果正确：
  - `bus.jpg` → 1 bus(0.86) + 4 person
  - `zidane.jpg` → 2 person + 2 tie

**遗留物（需要在麒麟机器上清理）**：
- **`xpu-probe` 容器仍在运行**（我建的探测容器，`sleep infinity`），清理命令：
  ```
  docker rm -f xpu-probe
  ```
- `/root/xpu-deploy-test/` 下有：`yolov5_dockerimg_v1.0.zip`、解压目录、
  `bus.jpg`、`zidane.jpg`、`test.jpg`（后者是我生成的合成测试图，可删）

---

## 10. 本轮已修复并提交的缺陷（commit `0b7ca2a`，9 files）

1. **交付脚本 CRLF** → 麒麟上 `sh build.sh` 报 `Syntax error: word unexpected`。
   修复：`template.py::render_to`、`config.py::dump_yaml`、`manifest.py::save`、
   `docker_exporter` 共 6 处显式 `newline="\n"`；新增 `.gitattributes`。
2. **无卡机器 `docker run` 必失败** → `build.sh.j2` 原硬编码 `--device=/dev/xpuctrl`，
   改为按 `[ -e /dev/xpuctrl ]` 探测，无卡自动降级 CPU 启动。
3. **`ENV MODEL_VERSION=custom`** → `manifest.py::build_manifest()` 硬编码 `"custom"`，
   已改为实际版本号。
4. **`'dict' object is not callable`** → `runtime/detection/runtime_detector.py` 里
   `self.preprocess = settings.PREPROCESS`（dict）**覆盖了同名方法 `preprocess()`**。
   已改名 `preprocess_cfg`。

---

## 11. 交付包依赖策略与仓库约定

- `configs/hardware/kunlun.yaml` 的 `packages_dir: "vendor/wheels"`，
  由 `DockerExporter._resolve_packages_dir` 解析（相对路径以仓库根为基准，
  目录不存在则视为未配置并写占位 `README.txt`）。
- `vendor/wheels/` 需 **13 个 wheel**：Flask / Werkzeug / Jinja2 / click / itsdangerous /
  gunicorn / minio / kafka_python / urllib3 / certifi / pytz / Cython / **onnxruntime**。
  - 缺 gunicorn → 容器崩溃重启（`ModuleNotFoundError: No module named 'gunicorn'`）。
  - 缺 onnxruntime → `/predict` 报 `code=-3`（降级 stub 产物走仿真加载必需）。
  - onnxruntime 选 **1.16.3 manylinux2014（cp310，6.1MB）**：与镜像内 numpy 1.26.x 的
    ABI 匹配，且导入只依赖 stdlib + numpy，适配 Dockerfile 的 `--no-deps`。
- **用户已决策**：`vendor/wheels/` 与 `images/yolov5-test-images/` **均不入库**，
  已在 `.gitignore` 中忽略并注明来源。**重新克隆后必须自行补齐 wheels**，
  否则交付包会静默缺依赖。
- `images/yolov5-test-images/{bus.jpg,zidane.jpg}` 是用户指定的接口验证素材。

---

## 12. 建议的恢复顺序

1. 在麒麟上 `docker rm -f xpu-probe`，清理遗留容器。
2. 在容器内装齐转换工具：`pip install x2paddle onnx sympy -i https://pypi.org/simple`。
3. 用现成的 ONNX（`output/onnx/*.onnx`）跑一次 `x2paddle` 转换，
   **先验证数值一致性**（Paddle 推理输出 vs onnxruntime 输出），再动代码。
4. 按 §6.1~6.4 改造代码（运行时优先，因为它是部署包的运行基础）。
5. 重新导出交付包 → 在麒麟上重建 → 复验 `/health` 与 `/predict`。
6. 拿到真实有卡机器后，只需确认 `paddle.device.xpu.device_count() > 0`
   即会自动走 XPU，无需改代码。
