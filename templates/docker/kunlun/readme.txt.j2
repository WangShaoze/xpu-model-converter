组件名称:
{{ model_name }}({{ package_version }})

一、手动安装(推荐):
1、加载基础镜像(查看Dockerfile中获取基础镜像信息)
$ docker load -i 基础镜像.tar

2、使用Dockerfile构建算法镜像
修改Dockerfile
    GPU_ID(显卡设备号,默认0)
    workers(并发数默认1,设置n后占用显存为n倍)
    HOST_IP(部署的服务器ip地址)
    MINIO_HOST(MINIO的ip地址)
    MINIO_PORT(MINIO的端口)
    MINIO_BUCKET(MINIO的bucket)
    MINIO_ACCESS_KEY(MINIO的access_key)
    MINIO_SECRET_KEY(MINIO的secret_key)
    KAFKA_HOST(kafka的ip地址)
    KAFKA_PORT(kafka的端口)
    KAFKA_TOPIC(kafka的topic)

确认信息正确后,构建算法镜像:
$ docker build -t {{ image_full }} .

3、运行镜像(昆仑芯 XPU 服务器)
$ device_param="--privileged --device=/dev/xpuctrl" && \
docker run -d \
-p {{ web_port }}:{{ web_port }} \
--name {{ container_name }} \
$device_param \
--restart=always {{ image_full }}
{% if degraded %}
!! 注意: 本次导出时未检测到昆仑 SDK, {{ algorithm_dir }}/{{ model_file }} 为占位产物(内容为规范化 ONNX),
   仅供链路联调, 请勿交付客户。请在具备昆仑 SDK 的环境中以 sdk_adapter=xpuctl 重新编译与导出。
{% endif %}
二、自动部署方式(可选):
修改Dockerfile的相关参数后,执行自动部署脚本
1.安装容器
功能: 删除旧版本的容器和镜像后进行算法安装
方式一: sh build.sh
方式二: sh build.sh 加-i或install或--install,功能与方式一一致
示例:
$ sh build.sh 会自动执行部署,并删除旧版本算法容器和镜像
$ sh build.sh -i
$ sh build.sh install
$ sh build.sh --install

2.删除容器
功能: 删除旧版本的容器和镜像
$ sh build.sh -d
$ sh build.sh delete
$ sh build.sh --delete

3.停止容器
功能: 停止当前算法容器
$ sh build.sh -stop
$ sh build.sh stop
$ sh build.sh --stop

4.重启容器
功能: 重启算法容器
$ sh build.sh -r
$ sh build.sh restart
$ sh build.sh --restart

5.查询用法
功能: 查询build.sh用法
$ sh build.sh -h
$ sh build.sh help
$ sh build.sh --help

三、测试:
1、测试(multipart/form-data方式)
$ curl http://localhost:{{ web_port }}/predict -F file=@./testimage.jpg
2、测试(urlpath方式)
$ curl -X POST http://localhost:{{ web_port }}/predict -H "Content-Type:application/json" --data '{"urlpath":"http://xxx/testimage.jpg"}'
3、测试(base64方式):
$ curl -X POST http://localhost:{{ web_port }}/predict -H "Content-Type:application/json" --data '{"base64":"/xdadd..."}'
4、测试(结果图方式)
$ curl -X POST http://localhost:{{ web_port }}/predict_image -F file=@./testimage.jpg
5、健康检查
$ curl http://localhost:{{ web_port }}/health

四、相关信息
1、并发与显存
当workers=n时显存占用为n倍
2、修改置信度和输出类别:
$ docker exec -it {{ container_name }} bash
修改 {{ algorithm_dir }}/confidence.json
说明: 输出类别与阈值在容器内 /usr/local/{{ algorithm_dir }}/confidence.json 中配置(共 {{ class_count }} 类)。
      code 字段为导出编号占位(1~{{ class_count }}), 客户侧按需替换为业务类别编码; 不需要输出的类别将 is_export 置 0。
3、日志上报(minio/kafka)
识别日志默认上报到容器内 send_log_webserver(端口 20000), 再转投 MINIO/KAFKA。
若未配置 MINIO_HOST/KAFKA_HOST(为空), 上报自动关闭, 不影响识别服务。
docker run -e 可覆盖: MINIO_HOST/MINIO_PORT/MINIO_BUCKET/MINIO_ACCESS_KEY/MINIO_SECRET_KEY/KAFKA_HOST/KAFKA_PORT/KAFKA_TOPIC/PROVINCE_CODE/BUREAU_CODE/REGION_CODE
4、交付包结构
Dockerfile             镜像构建文件
build.sh               自动部署脚本
readme.txt             本说明
packages/              额外依赖轮子(可为空)
{{ algorithm_dir }}/   算法目录(运行时代码 + 模型产物 + confidence.json + runtime.yaml + start.sh)

五、版本更新情况
{{ package_version }}: 由 xpu-model-converter {{ converter_version }} 导出生成
